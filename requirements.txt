# Parkinson's voice project dependencies.
# Install ONLY inside the project virtual environment (.venv).
#
# Where to run:  project folder, with .venv activated
# Command:       pip install -r requirements.txt

numpy>=2.0
scipy>=1.14
pandas>=2.2
scikit-learn>=1.5
librosa>=0.11
praat-parselmouth>=0.4.5
soundfile>=0.12
matplotlib>=3.9
joblib>=1.4
streamlit>=1.40
tqdm>=4.66
tabulate>=0.9
